[project]
name = "ecommerce-agent"
version = "0.1.0"
description = "Ecommerce operations agent exposed with the AgentCore Runtime HTTP contract"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "common",
    "fastapi[standard]>=0.129.0",
    "httpx>=0.27",
    "langfuse>=4.0.6",
    "strands-agents[a2a,otel]==1.48",
]

[tool.uv.sources]
common = { path = "../common", editable = true }
